# Supabase Configuration (Required)
# These are automatically provided by Lovable Cloud
VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-key"
VITE_SUPABASE_URL="https://your-project-id.supabase.co"

# Note: Private API keys (FIRECRAWL_API_KEY, ZAPIER_WEBHOOK_URL, etc.)
# are stored as Supabase secrets and not in this file.
# Access them in edge functions via Deno.env.get("SECRET_NAME")
